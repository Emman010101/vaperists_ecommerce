//put it as a global var so the class won't reset the value
//helpful when the user accidentally clicked on the wrong item
int selectedCategoryValue = 0;

//item count
int gItemCount = 0;

//this map is for storing the download url of the colors
var map = new Map();

//sms code
String smsCode = '';

//
